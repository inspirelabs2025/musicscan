name: Capgo OTA deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install
      - run: npm run build
      - run: npx @capgo/cli@latest bundle upload --channel production --apikey ${{ secrets.CAPGO_TOKEN }}
